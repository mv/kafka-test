# -*- mode: ruby -*-
# vi: set ft=ruby :

# Vagrantfile API/syntax version. Don't touch unless you know what you're doing!
VAGRANTFILE_API_VERSION = "2"

Vagrant.configure(VAGRANTFILE_API_VERSION) do |config|

# config.vm.box = "centos-6.4-x86_64-base-2013-11-23"
  config.vm.box = "http://s3-sa-east-1.amazonaws.com/download.mvway.com/box/kafka-standalone-chef.centos64_x86-64.box"

end

